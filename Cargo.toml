[workspace]
members = [
    "engine",
    "agent_runtime",
    "sandbox_runner",
]
default-members = ["engine"]
resolver = "3"

[profile.release]
codegen-units = 1
opt-level = 3
lto = true

[profile.dev]
debug = "line-tables-only"
opt-level = 1
